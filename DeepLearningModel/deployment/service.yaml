apiVersion: v1
kind: Service
metadata:
  name: i4q-demo-deployment
  namespace: deep-model
  labels:
    app: i4q-demo-deployment
    service: i4q-demo-deployment
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: i4q-demo-deployment

